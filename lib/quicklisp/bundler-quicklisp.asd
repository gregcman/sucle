(asdf:defsystem #:bundler-quicklisp
  :depends-on (#:quicklisp)
  :author "terminal625"
  :license "MIT"
  :description "bundle quicklisp libraries into the bundle folder for distribution"
  :components 
  ((:file "bundler-quicklisp"))) 

