application:
	"standalone" programs, what a person would interact with
		
component:
	macros, utility libraries, combinations of libraries...

data-structures:
	things which are simple enough that they would commonly be called "data structures"

common-lisp:
	language extension

ffi:
	foreign library bindings

forks:
	remixes/downloads of other people's libraries

unused:
	random code which is not necessarily maintained or functional

other:
	binaries, notes, c code